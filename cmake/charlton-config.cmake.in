@PACKAGE_INIT@

include(CMakeFindDependencyMacro)

# Find dependencies
find_dependency(Threads)

if(@CHARLTON_ENABLE_OPENMP@)
    find_dependency(OpenMP)
endif()

# Include targets
include("${CMAKE_CURRENT_LIST_DIR}/charlton-targets.cmake")

check_required_components(charlton)
